# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: taskUpload
on:
  push:
    branches: main
  workflow_dispatch: {}
jobs:
  upload:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      actions: write
    steps:
      - uses: actions/checkout@v2
        name: checkout
      - run: cd ./flywayjar
      - uses: actions/checkout@v2
        name: checkout2
      - uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: adopt
      - run: "gradle wrapper --gradle-version 6.7 "
      - uses: gradle/wrapper-validation-action@e6e38bacfdf1a337459f332974bb2327a31aaf4b
      - run: gradle build && gradle buildZip
      - name: run upload !
        run: aws s3 sync flywayjar/build/distributions/flywayjar-1.0-SNAPSHOT.zip
          s3://flywaymigrationconstruct
